const user = (state) => state.user;
const users = (state) => state.users;
const token = (state) => state.token;
const validatingToken = (state) => state.validatingToken;

export default {
  user,
  users,
  token,
  validatingToken,
};
